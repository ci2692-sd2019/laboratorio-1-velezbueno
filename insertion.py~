# Def de insertionsort

def insertionsort(lineas, n):

	for i in range(1, n):

		num = lineas[i]
		j = i - 1
		while(j >= 0 and lineas[i] > num):

			lineas[j+1] = lineas[j]
			j = j - 1

		lineas[j+1] = num

